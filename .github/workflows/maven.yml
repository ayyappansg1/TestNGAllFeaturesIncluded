# This workflow will build a Java project with Maven, and cache/restore any dependencies to improve the workflow execution time
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-java-with-maven

# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.

name: Java CI with Maven

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:

    runs-on: ubuntu-latest
    services:
      selenium-hub:
        image: selenium/hub:latest
        ports:
          - 4444:4444
      chrome:
        image: selenium/node-chrome:latest
        environment:
          HUB_HOST: selenium-hub
        depends_on:
          - selenium-hub
      firefox:
        image: selenium/node-firefox:latest
        environment:
          HUB_HOST: selenium-hub
        depends_on:
          - selenium-hub
      edge:
        image: selenium/node-edge:latest
        environment:
          HUB_HOST: selenium-hub
        depends_on:
          - selenium-hub

    steps:
    - uses: actions/checkout@v4
    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'
        cache: maven
    - name: Build with Maven
      run: mvn clean install
    - name: Run Tests in Docker
      run: |
        docker-compose up -d
        mvn test -P crossbrowser

    - name: Publish Test Results
      if: always()
      uses: actions/upload-artifact@v2
      with:
        name: test-results
        path: '**/surefire-reports/*.xml'
    - name: Tear down Docker Compose
      run: docker-compose down
    - name: Send Email with Test Results
      uses: dawidd6/action-send-mail@v3
      with:
          server_address: smtp.zoho.in
          server_port: 465
          username: ayyappansg1@zohomail.in
          password: FArvYhxi2Emk
          subject: "Test Results - ${{ github.run_id }}"
          body: "Hi Team, \nThe latest test results are available. Please see the attached reports."
          to: "ayyappangunasekaran5@gmail.com"
          attachments: "target/surefire-reports/*"

